@model List<ServiceGateway.Models.GameDTO>
@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

@foreach(var item in Model)
{
    <p>@item.Title</p>
    foreach(var g in item.Genres)
    {
        <p>@g.Name</p>
    }
}